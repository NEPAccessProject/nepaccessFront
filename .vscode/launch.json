{
    "configurations": [

      {
        "name": "Launch Chrome",
        "request": "launch",
        "type": "chrome",
        "url": "http://localhost:3000/",
        "webRoot": "${workspaceFolder}"

      },

        {
            "type": "chrome",
            "name": "Attach Chrome",
            "request": "attach",
            "port": 3000,
            "url": "http://localhost:3000/"
        }
    ]
}