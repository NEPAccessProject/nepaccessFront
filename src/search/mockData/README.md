This directory allows for the mocking of SOLR data.

To run the server install the json-server NPM package globally and then it the  ./mockData folder run `json-server --watch db.json --routes routes.json --port 8080`

json-server --watch db.json --routes routes.json --port 8080